import {createStore} from 'vuex'
import sensor from './modules/sensor'

export default createStore({
  state: {
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    sensor
  }
})
